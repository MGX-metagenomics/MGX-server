<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>MGX-maven</artifactId>
        <groupId>de.cebitec.mgx</groupId>
        <version>1.0</version>
    </parent>

    <groupId>de.cebitec.mgx</groupId>
    <artifactId>MGX-maven-ear</artifactId>
    <version>1.0</version>
    <packaging>ear</packaging>

    <name>MGX-maven-ear EAR</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <build>
        <plugins>
            <plugin>
                <!-- <groupId>org.n0pe.mojo</groupId> -->
                <groupId>org.codeartisans.asadmin</groupId>
                <artifactId>asadmin-maven-plugin</artifactId>
                <version>0.12</version>
                <executions>
                    <execution>
                        <phase>install</phase>
                        <!--                        <goals>
                            <goal>deploy</goal>
                        </goals>-->
                    </execution>
                </executions>
                <configuration>
                    <glassfishHome>${glassfish.home}</glassfishHome>
                    <host>${domain.host}</host>
                    <port>${domain.port}</port>
                    <user>${domain.user}</user>
                    <passwordfile>${domain.passfile}</passwordfile>
                    <appArchive>${project.build.directory}/${project.build.finalName}.${project.artifact.artifactHandler.extension}</appArchive>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.1</version>
                <configuration>
                    <source>1.7</source>
                    <target>1.7</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-ear-plugin</artifactId>
                <version>2.10.1</version>
                <configuration>
                    <version>6</version>
                    <defaultLibBundleDir>lib</defaultLibBundleDir>
                    <fileNameMapping>no-version-for-ejb</fileNameMapping>
                </configuration>
            </plugin>
        </plugins>
        <finalName>MGX-maven-ear</finalName>
    </build>
    
    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>MGX-maven-web</artifactId>
            <version>1.0</version>
            <type>war</type>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>MGX-gpms</artifactId>
            <version>${project.version}</version>
            <type>ejb</type>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>MGX-ExecutorService</artifactId>
            <version>${project.version}</version>
            <type>ejb</type>
        </dependency>
        <dependency>
            <artifactId>MGX-isequences</artifactId>
            <groupId>de.cebitec.mgx</groupId>
            <version>1.0</version>
        </dependency>
        <dependency>
            <artifactId>MGX-seqstorage</artifactId>
            <groupId>de.cebitec.mgx</groupId>
            <version>1.0</version>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>MGX-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>MGX-upload</artifactId>
            <version>${project.version}</version>
            <type>ejb</type>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>MGX-util</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>de.cebitec.mgx</groupId>
            <artifactId>MGX-controller</artifactId>
            <version>1.0</version>
            <type>ejb</type>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>MGX-jobsubmitter</artifactId>
            <version>${project.version}</version>
            <type>ejb</type>
        </dependency>
        <dependency>
            <groupId>de.cebitec.mgx</groupId>
            <artifactId>MGX-dispatcher-client</artifactId>
            <version>1.0</version>
            <type>ejb</type>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>MGX-global</artifactId>
            <version>${project.version}</version>
            <type>ejb</type>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>MGX-configuration</artifactId>
            <version>${project.version}</version>
            <type>ejb</type>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>MGX-statistics</artifactId>
            <version>${project.version}</version>
            <type>ejb</type>
        </dependency>
        <dependency>
            <groupId>de.cebitec.mgx</groupId>
            <artifactId>MGX-sessions</artifactId>
            <version>1.0</version>
            <type>ejb</type>
        </dependency>
    </dependencies>
    
    <profiles>
        <profile>
            <id>gpms-mysql</id>
            <dependencies>
                <dependency>
                    <groupId>de.cebitec.gpms</groupId>
                    <artifactId>GPMS-sql-loader</artifactId>
                    <version>1.1</version>
                    <type>ejb</type>
                </dependency>
            </dependencies>
        </profile>
        <profile>
            <id>gpms-ldap</id>
            <dependencies>
                <dependency>
                    <groupId>de.cebitec.gpms</groupId>
                    <artifactId>GPMS-ldap-loader</artifactId>
                    <version>1.1</version>
                    <type>ejb</type>
                </dependency>
            </dependencies>
        </profile>
    </profiles>
</project>
